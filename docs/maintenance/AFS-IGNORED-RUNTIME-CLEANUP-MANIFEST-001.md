# AFS Ignored Runtime Cleanup Manifest 001

Status: manifest for safe local cleanup during `AFS-MAINTENANCE-SLIMMING-001`.

## Cleanup Policy

Clean only ignored, reproducible local runtime residue. Do not delete preserved
evidence runs, model caches, local media, or user-provided project materials by
default.

## Safe Cleanup Targets

The following ignored paths are safe to remove locally after verification that
they resolve inside the repository:

```text
.pytest_cache/
**/__pycache__/
data/processed/pytest-basetemp/
```

## Preserve By Default

The following ignored paths may contain useful evidence, local dependencies, or
large user-managed assets and must not be removed by this maintenance pass:

```text
.venv/
data/models/faster-whisper/
data/processed/runs/
data/raw/
data/outputs/
data/reports/
```

## Git Boundary

The cleanup is local filesystem maintenance only. It should not stage generated
runtime files, remove committed examples, or affect deterministic tests beyond
removing stale cache residue.

## Completion Record

- Safe cleanup executed on 2026-06-03 in the
  `codex/afs-maintenance-slimming-001` maintenance worktree.
- Safety check: every target resolved inside the current worktree and passed
  `git check-ignore`.
- Initial cleanup removed 30 ignored cache targets:
  `.pytest_cache/` plus discovered `__pycache__/` directories under
  `agentflow/`, `apps/`, `narratocut/`, `narratostudio/`, and `tests/`.
- Final post-verification cleanup removed 34 ignored cache targets generated by
  the focused/full test runs.
- `data/processed/pytest-basetemp/` was not present in this worktree at cleanup
  time.
- Preserved by default: `.venv/`, `data/models/faster-whisper/`,
  `data/processed/runs/`, `data/raw/`, `data/outputs/`, and `data/reports/`.
