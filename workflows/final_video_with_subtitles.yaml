name: final_video_with_subtitles
version: 1
mode: final_video_with_subtitles
quality_profile: subtitle_burn
steps:
  - id: burn_subtitles
    type: burn_subtitles
    inputs:
      video: final_video_path
      subtitles: subtitles_path
    outputs:
      subtitle_burn_manifest: subtitle_burn_manifest.json
      final_video: final_video_with_subtitles.mp4
  - id: probe_subtitle_burn
    type: probe_subtitle_burn
    inputs:
      subtitle_burn_manifest: subtitle_burn_manifest
    outputs:
      subtitle_burn_manifest: subtitle_burn_manifest.json
