default_provider: mock

providers:
  mock:
    type: mock
    model: mock-local

  openai_compatible:
    type: openai_compatible
    base_url: ${NARRATOCUT_OPENAI_BASE_URL}
    api_key_env: NARRATOCUT_OPENAI_API_KEY
    model: ${NARRATOCUT_OPENAI_MODEL}
